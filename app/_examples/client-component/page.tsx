"use client";

// import useTasks from "@/__backengine__/hooks/useTasks";

// TODO: Duplicate, move this file outside the `_examples` folder and add your table to make it a route
export default function ClientComponent() {
  // this assumes you have a table called "tasks" in Backengine
  // const { tasks } = useTasks();

  return <pre></pre>;
  // return <pre>{JSON.stringify(tasks, null, 2)}</pre>;
}
